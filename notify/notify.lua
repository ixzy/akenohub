local basic = "AkenoHub"
function Notify(bruh,breh)
    game:GetService("StarterGui"):SetCore(
        "SendNotification",
        {
            Title = bruh,
            Text = breh
        }
    )
end
